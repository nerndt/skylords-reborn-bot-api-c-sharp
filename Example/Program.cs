
using Api;
using Bots;

// AspWrapper.Run(args, new Example.Example());
// AspWrapper.Run(args, new Bots.AIXanderFrostBot());

//List<IAspWrapperImpl> implementations = new List<IAspWrapperImpl>();
//implementations.Add(new Bots.AIXanderBot());
//implementations.Add(new Bots.AIXanderBot2());
//AspWrapperNew.Run(args, implementations); 

AspWrapper.Run(args, new Bots.AIXanderBot());
//AspWrapper.Run(args, new Bots.TryEverything());
